<Page x:Class="PostgresClient.View.FuncsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PostgresClient.View" xmlns:controls="clr-namespace:PostgresClient.Controls"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"    
      xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
      Title="FuncsView" >


    <Page.Resources>
        <Storyboard x:Key="Succsses" TargetProperty="Background.(SolidColorBrush.Color)" TargetName="RichBox">
            <ColorAnimation 
                    To="#004f04"  
                    AutoReverse="True"
                    FillBehavior="Stop" 
                    Duration="0:0:0.2"/>
        </Storyboard>
        <Storyboard x:Key="Fail" TargetProperty="Background.(SolidColorBrush.Color)" TargetName="RichBox">
            <ColorAnimation 
                    To="#b71c1c"  
                    AutoReverse="True"
                    FillBehavior="Stop" 
                    Duration="0:0:0.2"/>
        </Storyboard>
    </Page.Resources>


    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding PageLoaded, IsAsync=True}"/>
        </i:EventTrigger>
        
        <i:DataTrigger Binding="{Binding SuccssesSave, Mode=OneWay}" Value="True">
            <i:ControlStoryboardAction Storyboard="{StaticResource Succsses}"     
                                       ControlStoryboardOption="Play"/>
        </i:DataTrigger>
        <i:DataTrigger Binding="{Binding FailedSave, Mode=OneWay}" Value="True">
            <i:ControlStoryboardAction Storyboard="{StaticResource Fail}"     
                                       ControlStoryboardOption="Play"/>
        </i:DataTrigger>
    </i:Interaction.Triggers>

    <Grid>


        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="2"   Orientation="Horizontal">
            
            <Button x:Name="Save"  FontFamily="Segoe Fluent Icons"  Command="{Binding SaveCommand,IsAsync=True}" IsEnabled="{Binding IsEditable}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xe74e;" TextAlignment="Center" VerticalAlignment="Center" FontFamily="Segoe Fluent Icons" />
                    <TextBlock Text="Сохранить" TextAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
            
            <Button Command="{Binding ResetCommand}" IsEnabled="{Binding IsEditable}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xe777;" TextAlignment="Center" VerticalAlignment="Center" FontFamily="Segoe Fluent Icons" />
                    <TextBlock Text="Сбросить" TextAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
    
        </StackPanel>

        <DockPanel   Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" VerticalAlignment="Stretch">
            <Grid DockPanel.Dock="Bottom"  HorizontalAlignment="Stretch"  >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Button  Grid.Column="0" Grid.ColumnSpan="2"  Margin="5" HorizontalAlignment="Stretch"   Command="{Binding RemoveCommand}" IsEnabled="{Binding IsEditable}">
                    <StackPanel  Orientation="Horizontal">
                        <TextBlock Text="&#xe74d;" TextAlignment="Center" VerticalAlignment="Center" FontFamily="Segoe Fluent Icons" />
                        <TextBlock Text="Удалить" TextAlignment="Center" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

            </Grid>

            <ListView Background="#202020" DockPanel.Dock="Top" VerticalAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ItemsSource="{Binding FuncList}" SelectedIndex="{Binding Selected}"/>

        </DockPanel>
        <GridSplitter Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" Width="5" HorizontalAlignment="Center" Margin="5"/>
        <ScrollViewer Grid.Column="2" Grid.Row="1" >
            <controls:ExtendedRichTextBox VerticalScrollBarVisibility="Disabled" x:Name="RichBox" Text="{Binding FuncBody}"/>
        </ScrollViewer>
    </Grid>
</Page>
