<Page x:Class="PostgresClient.View.TableViewer"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PostgresClient.View" xmlns:controls="clr-namespace:PostgresClient.Controls" xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TableViewer"
      >

    <Grid Background="Transparent"  >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10,3,0,-5"   >
            <Grid>
                
                <ComboBox  x:Name="mybox" FontSize="20" IsDropDownOpen="{Binding IsDropDownOpened}" IsEnabled="{Binding IsConnected}" 
                      SelectedValue="{Binding SelectedTable,IsAsync=True,Mode=TwoWay}" ItemsSource="{Binding Tables}"  MaxWidth="120" BorderThickness="0" Margin="0,0,10,0"/>
                <TextBlock Text="Таблица" FontSize="20" FontFamily="Segoe UI" Foreground="LightGray" VerticalAlignment="Center"  Padding="5" IsHitTestVisible="False">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Hidden"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=mybox,Path=SelectedItem}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
            <Button  Command="{Binding DeleteRowCommand}" IsEnabled="{Binding IsEditable}">
                <StackPanel Margin="2"   Orientation="Horizontal">
                    <TextBlock Text="&#xe74d;" TextAlignment="Center" VerticalAlignment="Center" FontFamily="Segoe Fluent Icons" />
                    <TextBlock Text="Удалить строку" TextAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>



        <ScrollViewer  HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Disabled" Grid.Row="1"   >
            <controls:ExtendedDataGrid HeadersVisibility="Column" SelectedItem="{Binding SelectedRow}" ColumnWidth="*" VerticalAlignment="Stretch" 
                                       Width="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ScrollViewer}}, Path=ActualWidth}" 
                                       GridLinesVisibility="None"  Background="#202020" Foreground="White" Margin="5,10,0,0" Padding="5" FontSize="20" BorderThickness="0"
                                       HorizontalScrollBarVisibility="Auto" ItemsSource="{Binding TableContent}" IsEnabled="{Binding IsConnected}" IsReadOnly="{Binding IsNotEditable}">
            </controls:ExtendedDataGrid>

        </ScrollViewer>
       
    </Grid>
</Page>
